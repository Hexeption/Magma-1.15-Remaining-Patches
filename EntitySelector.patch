--- a/net/minecraft/server/EntitySelector.java
+++ b/net/minecraft/server/EntitySelector.java
@@ -64,7 +64,7 @@
     }
 
     private void e(CommandSource CommandSource) throws CommandSyntaxException {
-        if (this.checkPermissions && !CommandSource.hasPermission(2)) {
+        if (this.checkPermissions && !CommandSource.hasPermission(2, "minecraft.command.selector")) { // CraftBukkit
             throw ArgumentEntity.f.create();
         }
     }
